[pytest]
markers =
    pretrained: downloads a real pretrained checkpoint from the internet (slow, network-dependent) - excluded by default, run explicitly with `pytest -m pretrained`.
addopts = -m "not pretrained"
