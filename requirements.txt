keras>=3.0
numpy

# Choose one Keras 3 backend:
# tensorflow>=2.16
# jax[cpu]>=0.4
# torch>=2.1

# Only needed for weight porting (conversion-time only):
# torch>=2.1
# safetensors

# Only needed for keras_climate.weights.pretrained (downloading/reading
# real checkpoints; conversion-time only):
# torchvision>=0.16   # DeepLabV3+'s torchvision ResNet-50 ImageNet backbone
# transformers>=4.30  # SegFormer's HuggingFace ADE20K checkpoint
# pillow              # only needed for image I/O in your own inference code

# Required for keras_climate.datasets / keras_climate.samplers /
# keras_climate.transforms (a port of torchgeo.datasets/samplers/transforms):
# rasterio
# geopandas
# shapely
# pyproj
# fiona
# rtree
# matplotlib
# pillow
# einops

# Only needed by specific leaf datasets in keras_climate.datasets (lazily
# imported, so only required if you actually instantiate that dataset):
# h5py                 # CaBuAr, ChaBuD, CropHarvest, MMEarth, QuakeSet, SKIPPD, So2Sat, ZueriCrop, ...
# xarray, netCDF4, h5netcdf   # DL4GAMAlps, XarrayDataset
# pyarrow              # ClayEmbeddings, MajorTOMEmbeddings, geoparquet-backed datasets
# laspy                # IDTReeS (LiDAR point clouds)
# tokenizers           # SkyScript
# scipy                # ADVANCE (wavfile audio I/O)
# requests             # OpenAerialMap, OpenStreetMap

# To run the test suite or work on keras_climate itself, see
# requirements-test.txt instead (pip install -r requirements-test.txt).
